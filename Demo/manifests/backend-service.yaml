apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: devshop-demo
  labels:
    app: backend
    app.kubernetes.io/part-of: devshop
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
