# DevShop Frontend - Environment Variables
#
# No backend URL is required. The browser calls relative /api URLs on the same
# origin (port 5173). In production, Nginx reverse-proxies /api/* to the backend
# over the Docker network (backend:8080), so it works on localhost, any EC2 IP,
# or a domain without rebuild.
